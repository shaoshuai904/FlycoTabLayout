package com.flyco.tablayoutsamples.entity

import com.flyco.tablayout.listener.CustomTabEntity

class TabEntity(
        title: String,
        selectedIcon: Int,
        unselectedIcon: Int
) : CustomTabEntity(title, selectedIcon, unselectedIcon)